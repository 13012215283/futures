import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputArea: {
    position: 'absolute',
    width: '100%',
    paddingLeft: 10,
    paddingRight: 10,
  },
});

export default styles;
